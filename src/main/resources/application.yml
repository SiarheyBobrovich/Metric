management:
  metrics:
    enable:
      all: false
      http:
        server:
          requests: true
      product: true
      db: true
  endpoints:
    web:
      exposure:
        include: prometheus,health

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true